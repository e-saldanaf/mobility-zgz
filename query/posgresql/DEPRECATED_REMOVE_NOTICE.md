# DEPRECATION NOTICE

The directory `query/posgresql/` is deprecated. All SQL files have been moved to `query/postgresql/` (note the corrected spelling).

This branch commit removes the deprecated directory and its files to avoid confusion.

This change is safe to merge once maintainers confirm no external references to `query/posgresql/` remain.
